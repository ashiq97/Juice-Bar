 @model HomeViewModel

@await Html.PartialAsync("Carousel")

    <h2>Prefeeref Drinks of the Week</h2>
    <h4>Customers Preferences - just for you!</h4>

    <div class="row">
        @foreach (var drink in Model.PreferredDrinks)
        {
            @Html.Partial("DrinksSummary",drink)
        }
    </div>
